# Squad-like 战术 FPS 复刻方案

> 交付对象：Claude Code。把本文件放进空仓库根目录，重命名为 `PLAN.md`，让 Claude Code 逐里程碑执行。

---

## 0. 核心判断：先复制什么

Squad 的社区里流传一句话——它是**一款伪装成战术游戏的计分游戏**。真正让它区别于其他 FPS 的不是射击手感，而是这套东西：

**重生权是稀缺资源，而重生权由后勤决定。**

- 你死了，全队扣 1 张票（ticket）。票是有限的，归零即败。
- 你在哪重生，取决于班长有没有铺好 rally point、队伍有没有开卡车把补给拉到前线建 FOB/HAB。
- 所以一局比赛的胜负，是「谁的重生点离前线更近、活得更久」的经济学问题，不是枪法问题。

**结论：优先级排序应该是「规则经济 → 机器人验证 → 网络同步 → 第一人称射击 → 画面」，而不是反过来。**

绝大多数「我要做个 Squad」的项目死在第一步就去搓 3D 角色控制器和枪械后坐力，做了三个月做出一个没人想玩第二局的射击 demo。本方案强制反过来做。

---

## 1. 范围裁剪（必须接受的现实）

| 维度 | 真实 Squad | 本项目目标 |
|---|---|---|
| 人数 | 50v50 | **8v8 或 12v12**，缺口用 bot 填 |
| 地图 | 2×2 km ~ 4×4 km | **1×1 km** |
| 引擎 | Unreal Engine | TypeScript 全栈（见 §3） |
| 载具 | 几十种，含直升机 | 2 种：后勤卡车 + 轻装甲车 |
| 阵营 | 十几个，各自装备表 | 2 个对称阵营 |
| 语音 | 内建近距离 + 无线电 | Phase 5 可选，WebRTC |

裁剪理由：

1. **技术上限**：Godot 的内建复制在 32+ 玩家时会撞带宽和 CPU 瓶颈；即便自研 netcode，100 人权威服务器也是另一个量级的工程。
2. **社交动力学复制不了**：Squad 的乐趣有一半来自 9 人小队的语音协作和班长指挥。原型阶段找不到 100 个人陪你测，8v8 + bot 才是能反复迭代的规模。
3. **地图是最贵的资产**：Squad 的一张图是数月美术工作量。1km² 程序化生成 + 少量手摆地标是唯一可行路径。

---

## 2. 数值表（直接抄，这是最省事的「已验证设计」）

以下数值来自 Squad 官方 wiki 和社区指南。**游戏机制和数值不受版权保护，可以自由使用**；不可使用的是名字、Logo、地图、模型、音效、UI 美术（见 §8）。

### 2.1 Tickets（票数）

| 事件 | 票数变化 |
|---|---|
| 步兵阵亡（流血死 / 放弃 / 强制重生） | −1 |
| 指挥官阵亡 | −2 |
| FOB 电台被摧毁 | −20 |
| 载具损失 | 按类型（轻型 −5，装甲 −10~−20，自定） |
| 首次夺取一个从未被占领的点 | +20 |
| 全图控制点被一方占满 → 对方 **mercy bleed** | 60 秒内匀速扣 60 票 |
| 双方各自中和了对方的防守点（double neutral） | **所有 bleed 暂停**，直到僵局解除 |

起始票数建议 300（原版 AAS/RAAS 量级）。

### 2.2 控制点与 RAAS

- 两队从地图两端主基地出发，中间是一串控制点。
- **必须按顺序打**：没拿下前一个点，下一个点不可占领。地图上用 Attack / Defense 标记指示。
- **RAAS（Random AAS）**：每局从预设的多条「lane」（控制点连接图）里随机抽一条，开局不告诉玩家完整路线 —— 这是可玩性的关键，它逼迫侦察和临场决策。
- 占领判定：点内我方人数持续超过敌方一定时间 → 中和 → 占领。建议参数：半径 100m，中和 30s，占领 30s，人数差每多 1 人加速。

### 2.3 Rally Point（集结点，班级重生点）

- 由**班长**放置，需 ≥1 名队友在 8m 内（不同版本要求 1~2 人，取 1 人即可）。
- 消耗 **50 ammo**（从弹药包 / 载具 / FOB 弹药箱补充）。
- **只有本班能用**；有**波次冷却**（约每 60s 放一波人）。
- 不消耗建造点、不消耗票、不需要建造动作、可以捡回来重放。
- **子弹和爆炸打不坏**，只能靠敌人靠近来摧毁/禁用。
- 设计意图：这是「便宜但脆弱」的重生点，对应 FOB 的「昂贵但坚固」。两者的张力是整个战术层的发动机。

### 2.4 FOB / HAB（前进基地与重生掩体）

```
FOB 电台 (Radio Hub)
  ├─ 放置条件：班长 + 2 名队友在 15m 内
  ├─ 距其他友方 FOB ≥ 400m
  ├─ 距主基地 ≥ 150m
  ├─ 初始 0 CP（建造点）/ 0 AP（弹药点），必须靠后勤车运送
  ├─ 上限 20000 CP + 20000 AP
  └─ 建造半径 150m —— 半径内可造：
       ├─ HAB（重生掩体）      500 CP   ← 每个 FOB 限 1 个
       ├─ 弹药箱                100 CP
       ├─ 重机枪                200~350 CP
       ├─ 反坦克导弹            600 CP + 500 AP
       └─ 修理站                500 CP
  → 所以「一个能用的 FOB」最低成本 = 600 CP（HAB + 弹药箱）
```

**HAB 规则（最关键的一条）**：

- 全队都能在此重生，重生延迟 45s，无波次限制。
- **建造耗时随人数缩短**：1 人建需 40s 才能重生，5 人一起建只要 4s。→ 这条规则直接创造了「先建再打」的团队协作压力，务必实现。
- **Overrun（压制）**：敌方 2 人在 20m 内、或 8 人在 80m 内 → HAB 变红，禁止重生，直到敌人被清掉。电台受损也会导致 overrun。
- 电台被摧毁 → 半径内所有科技建筑（HAB、弹药箱、修理站）和武器平台**瞬间一起消失**；沙袋等纯工事保留。
- 自己主动拆掉电台不扣票 —— 阵地暴露时的正确处置。

### 2.5 后勤（Logistics）

- 后勤卡车在主基地装载补给，由驾驶员自选 CP / AP 配比（典型 1200 CP + 1800 AP，够建 2 个 FOB）。
- 开到前线，在 FOB 建造半径内卸货。也可以 FOB 之间互相转运。
- 车辆移动中不能装卸。
- **这是整个游戏的心跳**。后勤跑得好不好，直接决定胜负。bot 必须会跑后勤，否则平衡性测试全是假的。

---

## 3. 技术栈

### 推荐：TypeScript 单仓（monorepo）

```
squad-like/
├─ packages/
│  ├─ core/        纯规则引擎：无渲染、无网络、无 I/O、确定性
│  ├─ server/      权威服务器：Node + uWebSockets.js + Rapier3D
│  ├─ client/      Three.js 客户端（先 2D 俯视，后 3D 第一人称）
│  ├─ bots/        机器人 AI（服务端跑）
│  └─ sim/         无头批量对战 + 平衡性统计 CLI
├─ CLAUDE.md
└─ PLAN.md         （本文件）
```

**为什么是 TypeScript 而不是 Godot / Unreal：**

| | TypeScript + Three.js | Godot 4 | Unreal 5 |
|---|---|---|---|
| Claude Code 可驱动性 | ★★★ 全文本、可单测、可无头跑 | ★★ .tscn 场景文件需要编辑器介入 | ★ Blueprint 基本不可自动化 |
| Linux 原生 | ★★★ | ★★★ | ★ 痛苦 |
| 无头自动化测试 | ★★★ vitest 秒级跑千局 | ★★ `godot --headless` 可用但慢 | ★ |
| 3D 画面上限 | ★★ | ★★★ | ★★★ |
| 网络同步 | 自己写（可控） | 内建复制 32 人以上撑不住，需上 netfox | ★★★ 工业级 |
| 分享给别人玩 | ★★★ 发个 URL | ★★ 要发包 | ★ |

关键权衡：**你要的是「能被 Claude Code 高效迭代出来的可玩系统」，不是「画面达标的商业 FPS」。** TypeScript 在前者上碾压，在后者上不行。

**换栈退路**：`packages/core` 必须是纯逻辑、零依赖、规则常量集中在一个 `rules.ts`。如果将来要迁到 Godot，这一层可以用 GDScript 或 C# 一比一重写，或者干脆让 Godot 客户端连 Node 服务器。**这是本方案里唯一不能妥协的架构约束。**

### 关键库

- **物理 / 弹道**：`@dimforge/rapier3d-compat`（服务端客户端同一份，WASM）
- **网络**：先 `uWebSockets.js`（WebSocket，够用），Phase 4 若延迟成瓶颈再换 `geckos.io`（WebRTC unreliable UDP）
- **渲染**：`three`（当前 r184+）
- **测试**：`vitest` + `fast-check`（属性测试）
- **可参考的开源实现**（读架构，不要直接 fork）：
  - `MavonEngine/Core` —— Three.js + Rapier3D + geckos.io，服务端客户端共享实体类，Source 引擎式 tick 命令缓冲，是本方案最接近的现成参考
  - `iErcann/Notblox` —— Three.js + Node 权威服务器，含载具物理
  - `foxssake/netfox` —— 若最终选 Godot，客户端预测/回滚就用它

---

## 4. 网络模型

标准三件套，照 Valve/Source 的做法：

1. **服务器权威，20Hz tick**。所有规则判定只在 `core` 里跑一次。
2. **客户端预测**：本地立即响应输入，同时把带序号的输入发给服务器。
3. **服务器和解 + 回放**：服务器回包带「已处理的最后一个输入序号」，客户端接受权威状态后重放未确认的输入。
4. **其他玩家插值**：渲染在过去 100ms，平滑。
5. **命中判定的延迟补偿**：服务器为每个玩家保留 ~1s 的位置历史，开枪时把世界回滚到该客户端当时看到的时刻再判定。

带宽预算：**按距离剔除实体**（500m 外不发送），只发变化量（脏位检测 + 哈希）。12 人规模下每客户端目标 < 30 KB/s。

---

## 5. 里程碑（这是给 Claude Code 的任务分解）

每个里程碑必须有**可运行的验收标准**，不达标不进下一阶段。

### M0 — 规则引擎（无渲染、无网络）

**产出**：`packages/core` + `packages/sim`，一条 CLI 命令跑完整一局比赛并输出文字战报。

- [ ] `rules.ts`：§2 全部常量集中于此，一个 magic number 都不许散落在别处
- [ ] 实体模型：Player / Squad / Team / ControlPoint / RallyPoint / FOB / Deployable / Vehicle / Supply
- [ ] 状态机：票数、控制点占领（含 double neutral 与 mercy bleed）、RAAS lane 随机抽取
- [ ] FOB 全生命周期：放置校验（400m/150m/15m 三个距离约束）→ 补给 → 建造（人数加速）→ overrun 判定 → 摧毁级联
- [ ] Rally point：放置、弹药消耗、波次冷却、敌人靠近禁用
- [ ] 后勤：装载配比、运输、卸货、FOB 间转运
- [ ] **确定性**：`core` 内禁止 `Math.random()` / `Date.now()`，全部走注入的 seeded RNG 和 tick 计数。同一 seed + 同一输入序列必须产出逐 tick 完全一致的状态。
- [ ] 单元测试覆盖每一条 §2 规则；`fast-check` 属性测试保证不变量（票数单调、CP 不为负、FOB 距离约束永不违反）

**验收**：`pnpm sim --seed 42` 输出一局完整战报（每分钟票数、控制点易手、FOB 建毁记录），可复现。

### M1 — 机器人与平衡性

**产出**：`packages/bots` + 批量对战统计。

- [ ] 分层 bot：士兵（移动/交火/建造）、班长（放 rally、选择 FOB 位置）、后勤兵（跑补给循环）
- [ ] 简化战斗解算（此阶段不需要弹道，用命中概率模型）
- [ ] `pnpm sim --matches 1000` 输出：平均局时、票数曲线、FOB 平均寿命、rally 使用率、胜负分布

**验收**：1000 局无崩溃、无死锁；平均局时落在 30~60 分钟区间；双方胜率 45%~55%。**如果不满足，说明规则数值抄错了或实现有 bug，回 M0 修，不要往前走。**

### M2 — 权威服务器 + 2D 可玩客户端

**产出**：真人能连进去和 bot 混战的 2D 俯视版本。

- [ ] 服务器：20Hz tick，输入队列，状态快照，按距离剔除
- [ ] 客户端：Canvas 2D 俯视图（就是 Squad 的地图界面），WASD 移动，点击射击，班长指令面板
- [ ] 人机混编：真人占位，其余全 bot
- [ ] 网络诊断 HUD：ping、带宽、tick 偏差、预测误差

**验收**：4 个真人 + 12 个 bot 打完一局，全程无掉线、无状态漂移。这时候游戏**已经能玩了** —— 这是本方案最重要的一个节点，它证明了乐趣来自规则而非画面。

### M3 — 3D 第一人称

- [ ] Three.js 场景 + Rapier 角色控制器 + 第一人称相机
- [ ] 武器：射速、后坐力、弹道下坠、换弹、瞄具
- [ ] 服务端延迟补偿命中判定
- [ ] 受伤 / 倒地 / 拖拽 / 医疗救助（Squad 的倒地系统给了队友救援窗口，是团队黏合剂，必须做）
- [ ] 压制效果：子弹从身边飞过 → 视野模糊 + 后坐力上升。**这是 Squad 手感的灵魂，比枪械模型重要得多。**
- [ ] 程序化地形 1km² + 少量手摆地标建筑

### M4 — 载具与后勤

- [ ] 后勤卡车（Rapier 车辆物理）+ 轻装甲车
- [ ] 装载 / 卸载 UI，多人载具座位
- [ ] 载具损毁 → 扣票；修理站

### M5 — 团队协作层（可选）

- [ ] WebRTC 近距离语音 + 班长频道
- [ ] 指挥官角色：全局资源、空投补给、侦察无人机
- [ ] 地图标记系统

---

## 6. 给 Claude Code 的 `CLAUDE.md`（直接用这份）

```markdown
# 项目约束（不可违反）

## 架构
- `packages/core` 是纯函数规则引擎：不得 import 任何渲染、网络、文件系统、
  计时器相关模块。不得使用 Math.random()、Date.now()、performance.now()。
  随机数只能来自注入的 seeded RNG；时间只能来自 tick 计数。
- 所有游戏数值常量必须定义在 `packages/core/src/rules.ts`。
  在其他文件中出现裸数字（0/1/-1 除外）视为 bug。
- 服务器是唯一权威。客户端可以预测，但客户端的计算结果永远不写回权威状态。

## 不变量（每次改动后必须仍然成立，写成属性测试）
1. 任一时刻，任一队伍票数 ≥ 0 且单调不增（除明确的加票事件）
2. FOB 之间距离恒 ≥ 400m；FOB 距主基地恒 ≥ 150m
3. FOB 的 CP / AP 恒在 [0, 20000] 区间
4. 控制点占领顺序恒符合当前 lane 的拓扑约束
5. 同一 seed + 同一输入序列 → 逐 tick bit-identical 的状态哈希

## 测试门禁
每次提交前必须通过：
- `pnpm test`（单测 + 属性测试）
- `pnpm sim --matches 100`（100 局无头对战，零崩溃、零死锁、
  平均局时在 30-60 分钟区间）
状态哈希写入快照文件，回归时对比。

## 禁止
- 不得使用 "Squad" 作为项目名、模块名、类名或任何用户可见文本
- 不得引入任何来自 Squad 或其他商业游戏的美术、音频、地图数据
- 不要为了让测试通过而修改测试或放宽不变量；先修实现
```

---

## 7. 工作方式建议

**让 Claude Code 高效的三个关键：**

1. **无头可验证**。M0/M1 的整个价值就在于：Claude Code 能自己跑 `pnpm sim --matches 100`，从统计输出里看出规则实现错了，然后自己修。没有这一层，它就只能靠你肉眼看画面反馈，迭代速度掉一个数量级。

2. **确定性 + 状态哈希**。每局记录 seed 和逐 tick 状态哈希。出 bug 时给 Claude Code 一个 seed 它就能完整复现，这比任何 bug 描述都有效。

3. **一次一个里程碑，写完就验收**。不要让它同时动 core 和 client。M0 做完，你自己跑一遍战报，觉得「这个数值曲线像 Squad」，再放行 M1。

**时间预期**（Claude Code 主力开发，你做验收）：M0 约 2-4 天，M1 约 3-5 天，M2 约 1-2 周，M3 约 3-4 周。M2 结束时你就有一个能约朋友玩的东西了。

---

## 8. 法律边界

- **游戏机制、规则、数值不受版权保护。** 你可以逐条复制 Squad 的 FOB 规则和票数表，这在法律上和复制象棋规则没有区别。
- **不能碰的**：名称 "Squad"、Logo、地图（Gorodok、Fools Road 等）、3D 模型、贴图、音效、UI 美术、代码。
- **商标**：项目起一个自己的名字，别叫 "Squad Clone" 或 "OpenSquad"。README 里写 "inspired by Squad and Project Reality" 是安全且常见的做法。
- 如果只是自己玩 / 开源不商业化，风险极低。要商业化的话，那一步之前找律师看一眼。

---

## 9. 三个最容易翻车的地方

1. **跳过 M0/M1 直接做 3D。** 会做出一个手感还行但没人想打第二局的射击 demo。Squad 的乐趣 90% 在规则层。
2. **规模不肯砍。** 想做 50v50，netcode 复杂度上升一个量级，然后卡在那里再也出不来。
3. **core 层被污染。** 一旦有人图省事在规则引擎里 `import three` 或者调 `Date.now()`，确定性没了，无头测试就废了，整套自动化迭代随之崩塌。这是唯一需要你亲自守住的红线。
